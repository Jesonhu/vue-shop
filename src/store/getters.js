export const sku = state => state.sku;

export const skuStatus = state => state.skuStatus;

export const skuGoods = state => state.skuGoods;

export const skuGoodsId = state => state.skuGoodsId;

export const chosenContactId = state => state.chosenContactId;

export const loginModal = state => state.loginModal;

export const userInfo = state => state.userInfo;

export const shopcartCount = state => state.shopcartCount;

export const checkOrder = state => state.checkOrder;

export const createOrder = state => state.createOrder;

export const area = state => state.area;

export const reviseAddress = state => state.reviseAddress;

export const selectAddress = state => state.selectAddress;

export const orderActive = state => state.orderActive;
